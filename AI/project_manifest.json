{
    "project": "LightchangerT",
    "version": "2.2.0",
    "description": "Network-based gaming console detection with LED color feedback on Raspberry Pi/Linux with remote ESP32 control",
    "branch_type": "python",
    "platform": {
        "primary": "Raspberry Pi",
        "secondary": "General Linux SoC",
        "language": "Python 3.8+",
        "os_requirements": "Linux with arp, ping commands available"
    },

    "pipeline": {
        "setup": [
            "Install Python 3.8+",
            "Run: pip install -r requirements.txt (optional dependencies)",
            "Configure config.json with network settings and LED hardware",
            "Test: python3 main.py"
        ],
        "testing": [
            "python3 -m py_compile *.py",
            "python3 -m pytest tests/ -v"
        ],
        "build": "Native Python - no build step required",
        "deploy": [
            "Copy files to target machine",
            "Set execution bit: chmod +x main.py",
            "Run manually: python3 main.py",
            "Or as systemd service: systemctl start lightchanger (see lightchanger.service)"
        ]
    },

    "sync_instructions": {
        "description": "Guidelines for syncing features between Python (main) and ESP32 branches",
        "principles": [
            "Each branch maintains independent implementation appropriate for its platform",
            "Feature parity is goal, not exact code parity",
            "Manifest updates should be mirrored to both branches",
            "Detection logic (MAC_OUI, STATIC_LIST, PORT_PROBE, MDNS) should be consistent"
        ],
        "when_to_sync": [
            "Adding new device detection methods",
            "Adding new supported brands and colors",
            "Changing detection logic/priority algorithms",
            "Adding new configuration options",
            "Fixing bugs that apply to both platforms",
            "UI/UX improvements to web config interface",
            "Remote command channel protocol updates"
        ],
        "sync_process": {
            "step1": "Implement feature on originating branch",
            "step2": "Test thoroughly on that platform (compile + verify)",
            "step3": "Port logic to other branch (adapt code, not copy-paste)",
            "step4": "Test on second platform",
            "step5": "Update both manifests to reflect new feature",
            "step6": "Commit with descriptive message mentioning both branches"
        },
        "do_not_sync": [
            "Platform-specific hardware code (LED drivers)",
            "Network stack differences (ARP vs WiFi)",
            "Memory management differences",
            "Build system configurations",
            "Language-specific patterns (Python threads vs Arduino loops)"
        ]
    },

    "architecture": {
        "components": {
            "main.py": "Entry point, GameStateController, main loop with error recovery and optional ESP32 remote mirror",
            "led_controller.py": "LED color management with factory pattern (FastLED/NeoPixel/RPi_WS281x)",
            "colors.py": "Shared COLOR_MAP and Color namedtuple definitions",
            "esp32_client.py": "TCP client for sending color commands to remote ESP32 (COLOR:name, RGB:r,g,b, OFF)",
            "scanner.py": "Network device discovery via ARP table + static list",
            "config_manager.py": "Thread-safe singleton config loader with RLock and atomic writes",
            "liveness.py": "ICMP ping validation via subprocess",
            "steam_detector.py": "Steam detection via port probe or mDNS (zeroconf)",
            "web_config.py": "HTTP configuration UI with JSON editor and atomic writes"
        },
        "flow": "Scan ARP table -> Verify Liveness (ping) -> Check Steam port -> Determine Priority -> Set LED [-> Mirror to ESP32 if configured]"
    },

    "remote_command": {
        "description": "TCP-based one-shot command channel for remote ESP32 LED control",
        "protocol": "Text-based, newline-delimited. Commands: COLOR:name, RGB:r,g,b, OFF, STATUS?",
        "responses": "OK, ERR:UNKNOWN, ERR:INVALID_RGB, STATUS:state",
        "default_port": 10001,
        "config_section": "esp32_command in config.json",
        "esp32_side": "config.h COMMAND_PORT_DEFAULT, config_manager.h get/setCommandPort(), esp32.ino WiFiServer on command port",
        "python_side": "esp32_client.py Esp32CommandClient class"
    },

    "detection": {
        "methods": ["MAC_OUI", "STATIC_LIST", "PORT_PROBE", "MDNS"],
        "validation": "ICMP_Ping via subprocess",
        "deep_sleep_handling": "Considered offline if ping fails",
        "priority_logic": "Last online device wins",
        "device_management": "Web UI with JSON editor for static devices and colors"
    },

    "color_mapping": {
        "sony": "blue",
        "microsoft": "green",
        "nintendo": "red",
        "steam": "light_blue",
        "nvidia": "light_green",
        "default": "white"
    },

    "web_config": {
        "enabled_via_config": "config.json network.web_config_enabled",
        "default_port": 80,
        "endpoints": [
            "GET / - Main configuration UI with JSON editor",
            "POST /save - Save configuration (atomic write via tempfile)",
            "GET /api/config - Get command port setting",
            "GET /api/devices - List all devices",
            "GET /api/device?idx=N - Get device by index",
            "DELETE /api/device?idx=N - Delete device by index",
            "Fields: network_scan_interval_seconds, network_detection_mode, devices_static_list, colors, hardware, cmd_port"
        ],
        "security_notes": [
            "No authentication - only for trusted networks",
            "Atomic writes prevent config corruption"
        ]
    },

    "hardware": {
        "supported_led_libraries": ["FASTLED", "NEOPIXEL", "RPI_WS281X"],
        "config_fields": ["led_pin", "num_leds", "brightness"]
    },

    "dependencies": {
        "core": ["zeroconf>=4.2.0"],
        "optional": [],
        "testing": ["pytest>=7.0.0"],
        "installation": "pip install -r requirements.txt"
    },

    "deployment": {
        "systemd_service": "lightchanger.service",
        "install": "sudo cp lightchanger.service /etc/systemd/system/ && systemctl daemon-reload && systemctl enable --now lightchanger"
    },

    "current_phase": "Production",
    "last_updated": "2026-05-12",
    "changes": {
        "bug_fixes": [
            "conftest.py - removed non-existent SteamServiceListener import",
            "esp32.ino - fixed DeviceConfig/Device type mismatch in isValidDevice call",
            "esp32.ino - replaced blocking delay(1000) with millis() non-blocking WiFi reconnect",
            "steam_detector.h - reduced delay(100) to delay(10) between IP probes",
            "led_controller.py - removed unused import json, added module-level logger",
            "requirements.txt - added zeroconf>=4.2.0 as required dependency"
        ],
        "improvements": [
            "Created colors.py shared module with COLOR_MAP and Color namedtuple",
            "main.py imports COLOR_MAP from colors.py instead of duplicating",
            "config_manager.py uses atomic writes (tempfile.mkstemp + os.replace)",
            "Added .gitignore, tests/__init__.py, pytest>=7.0.0 to requirements.txt",
            "Added systemd service file (lightchanger.service)",
            "Added CI workflow (.github/workflows/ci.yml) with py_compile, pytest, ESP32 compile",
            "Expanded README with install, API docs, wiring guide, and CI documentation"
        ],
        "new_features_v2.2": [
            "Remote ESP32 command channel via TCP (esp32_client.py + esp32.ino commandServer)",
            "Protocol: COLOR:name, RGB:r,g,b, OFF, STATUS? - one-shot TCP, newline-delimited",
            "Web UI command port setting with NVS persistence and reboot warning",
            "Optional dual-mode: local LED + remote ESP32 mirror via esp32_command config",
            "config_manager.h: getCommandPort/setCommandPort with NVS storage",
            "config.json: esp32_command section (enabled, host, port)"
        ]
    }
}